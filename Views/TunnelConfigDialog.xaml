<Window x:Class="MasselGUARD.Views.TunnelConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:MasselGUARD"
        Title="{Binding [TunnelDialogAddTitle], Source={x:Static local:Lang.Instance}}"
        Width="560" Height="640"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner">

    <Border Background="{DynamicResource WindowBg}"
            BorderBrush="{DynamicResource Accent}"
            BorderThickness="1" CornerRadius="{DynamicResource Theme.CornerRadius}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44"/>   <!-- Title bar -->
                <RowDefinition Height="Auto"/> <!-- Tunnel name -->
                <RowDefinition Height="*"/>    <!-- Tabs -->
                <RowDefinition Height="Auto"/>   <!-- Button bar -->
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border Grid.Row="0" Background="{DynamicResource Surface}"
                    CornerRadius="{DynamicResource Theme.CornerRadiusTop}"
                    MouseLeftButtonDown="TitleBar_MouseDown">
                <Grid>
                    <TextBlock x:Name="DialogTitle"
                               FontFamily="{DynamicResource Theme.FontFamily}" FontSize="13" FontWeight="Bold"
                               Foreground="{DynamicResource Accent}"
                               VerticalAlignment="Center" Margin="16,0"/>
                    <Button Content="✕" Style="{StaticResource DangerBtn}"
                            HorizontalAlignment="Right" Padding="10,4" Margin="0,0,8,0"
                            BorderThickness="0" Background="Transparent"
                            Click="CancelBtn_Click"/>
                </Grid>
            </Border>

            <!-- Tunnel name — above tabs, always visible -->
            <Border Grid.Row="1"
                    Background="{DynamicResource WindowBg}"
                    BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1"
                    Padding="16,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="160"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"
                               Text="{Binding [TunnelDialogName], Source={x:Static local:Lang.Instance}}"
                               Style="{StaticResource FormLabel}"
                               VerticalAlignment="Center"
                               Margin="0,0,12,0"/>
                    <TextBox Grid.Column="1" x:Name="NameBox"
                             FontFamily="{DynamicResource Theme.FontFamily}" FontSize="12"/>
                    <TextBlock Grid.Column="2"
                               Text="{Binding [TunnelDialogGroup], Source={x:Static local:Lang.Instance}}"
                               Style="{StaticResource FormLabel}"
                               VerticalAlignment="Center"
                               Margin="12,0,8,0"/>
                    <ComboBox Grid.Column="3" x:Name="GroupPicker"
                              FontFamily="{DynamicResource Theme.FontFamily}" FontSize="11"/>
                </Grid>
            </Border>

            <!-- Tab control: Fields / Raw -->
            <TabControl Grid.Row="2" x:Name="Tabs"
                        Background="{DynamicResource WindowBg}"
                        BorderBrush="{DynamicResource BorderColor}"
                        BorderThickness="0"
                        Margin="0">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="FontFamily" Value="Consolas"/>
                        <Setter Property="FontSize"   Value="11"/>
                        <Setter Property="Padding"    Value="14,7"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border x:Name="TabBorder"
                                            Background="{DynamicResource Surface}"
                                            BorderBrush="{DynamicResource BorderColor}"
                                            BorderThickness="1,1,1,0"
                                            CornerRadius="4,4,0,0"
                                            Margin="0,0,2,0"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter x:Name="TabLabel"
                                                          ContentSource="Header"
                                                          TextBlock.Foreground="{DynamicResource TextMuted}"
                                                          TextBlock.FontFamily="{DynamicResource Theme.FontFamily}"
                                                          TextBlock.FontSize="11"
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="TabBorder" Property="Background"
                                                    Value="{DynamicResource WindowBg}"/>
                                            <Setter TargetName="TabBorder" Property="BorderBrush"
                                                    Value="{DynamicResource Accent}"/>
                                            <Setter TargetName="TabLabel"
                                                    Property="TextBlock.Foreground"
                                                    Value="{DynamicResource Accent}"/>
                                            <Setter TargetName="TabLabel"
                                                    Property="TextBlock.FontWeight"
                                                    Value="Bold"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="TabBorder" Property="Background"
                                                    Value="{DynamicResource CardBg}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>

                <!-- Fields tab -->
                <TabItem x:Name="TabFields"
                         Header="{Binding [TunnelDialogTabForm], Source={x:Static local:Lang.Instance}}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20,16">
                        <StackPanel>

                            <!-- INTERFACE -->
                            <TextBlock Text="{Binding [TunnelDialogInterface], Source={x:Static local:Lang.Instance}}"
                                       Style="{StaticResource SectionLabel}" Margin="0,0,0,8"/>

                            <TextBlock Text="{Binding [TunnelDialogPrivateKey], Source={x:Static local:Lang.Instance}}"
                                       Style="{StaticResource FormLabel}"/>
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="PrivateKeyBox" Grid.Column="0" Margin="0,0,6,0"
                                         FontFamily="{DynamicResource Theme.FontFamily}" FontSize="10"/>
                                <Button Grid.Column="1"
                                        Content="{Binding [BtnGenerateKey], Source={x:Static local:Lang.Instance}}"
                                        Style="{StaticResource FlatBtn}" FontSize="10" Padding="10,6"
                                        Click="GenerateKey_Click"/>
                            </Grid>

                            <TextBlock Text="{Binding [TunnelDialogAddress], Source={x:Static local:Lang.Instance}}"
                                       Style="{StaticResource FormLabel}"/>
                            <TextBox x:Name="AddressBox" Margin="0,0,0,10" FontFamily="{DynamicResource Theme.FontFamily}" FontSize="11"/>

                            <TextBlock Text="{Binding [TunnelDialogDns], Source={x:Static local:Lang.Instance}}"
                                       Style="{StaticResource FormLabel}"/>
                            <TextBox x:Name="DnsBox" Margin="0,0,0,10" FontFamily="{DynamicResource Theme.FontFamily}" FontSize="11"/>

                            <TextBlock Text="{Binding [TunnelDialogListenPort], Source={x:Static local:Lang.Instance}}"
                                       Style="{StaticResource FormLabel}"/>
                            <TextBox x:Name="ListenPortBox" Margin="0,0,0,10" FontFamily="{DynamicResource Theme.FontFamily}" FontSize="11"/>

                            <TextBlock Text="{Binding [TunnelDialogMtu], Source={x:Static local:Lang.Instance}}"
                                       Style="{StaticResource FormLabel}"/>
                            <TextBox x:Name="MtuBox" Margin="0,0,0,14" FontFamily="{DynamicResource Theme.FontFamily}" FontSize="11"/>

                            <!-- PEER -->
                            <TextBlock Text="{Binding [TunnelDialogPeer], Source={x:Static local:Lang.Instance}}"
                                       Style="{StaticResource SectionLabel}" Margin="0,4,0,8"/>

                            <TextBlock Text="{Binding [TunnelDialogPublicKey], Source={x:Static local:Lang.Instance}}"
                                       Style="{StaticResource FormLabel}"/>
                            <TextBox x:Name="PublicKeyBox" Margin="0,0,0,10" FontFamily="{DynamicResource Theme.FontFamily}" FontSize="10"/>

                            <TextBlock Text="{Binding [TunnelDialogPresharedKey], Source={x:Static local:Lang.Instance}}"
                                       Style="{StaticResource FormLabel}"/>
                            <TextBox x:Name="PresharedKeyBox" Margin="0,0,0,10" FontFamily="{DynamicResource Theme.FontFamily}" FontSize="10"/>

                            <TextBlock Text="{Binding [TunnelDialogEndpoint], Source={x:Static local:Lang.Instance}}"
                                       Style="{StaticResource FormLabel}"/>
                            <TextBox x:Name="EndpointBox" Margin="0,0,0,10" FontFamily="{DynamicResource Theme.FontFamily}" FontSize="11"/>

                            <TextBlock Text="{Binding [TunnelDialogAllowedIPs], Source={x:Static local:Lang.Instance}}"
                                       Style="{StaticResource FormLabel}"/>
                            <TextBox x:Name="AllowedIPsBox" Margin="0,0,0,10" FontFamily="{DynamicResource Theme.FontFamily}" FontSize="11"/>

                            <TextBlock Text="{Binding [TunnelDialogKeepalive], Source={x:Static local:Lang.Instance}}"
                                       Style="{StaticResource FormLabel}"/>
                            <TextBox x:Name="KeepaliveBox" Margin="0,0,0,10" FontFamily="{DynamicResource Theme.FontFamily}" FontSize="11"/>

                            <!-- SCRIPTS -->
                            <TextBlock Text="{Binding [TunnelScriptsHeader], Source={x:Static local:Lang.Instance}}"
                                       Style="{StaticResource SectionLabel}" Margin="0,8,0,8"/>
                            <TextBlock Text="{Binding [TunnelScriptsInfo], Source={x:Static local:Lang.Instance}}"
                                       FontFamily="{DynamicResource Theme.FontFamily}" FontSize="9"
                                       Foreground="{DynamicResource TextMuted}" TextWrapping="Wrap"
                                       Margin="0,0,0,14"/>

                            <!-- Pre-connect -->
                            <TextBlock Text="{Binding [TunnelScriptPreConnect], Source={x:Static local:Lang.Instance}}"
                                       FontFamily="{DynamicResource Theme.FontFamily}" FontSize="10"
                                       FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"
                                       Margin="0,0,0,4"/>
                            <Grid Margin="0,0,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" x:Name="PreConnectBox"
                                         FontFamily="{DynamicResource Theme.FontFamily}" FontSize="11"
                                         Padding="6,4"/>
                                <Button Grid.Column="1" Content="{Binding [BtnBrowse], Source={x:Static local:Lang.Instance}}"
                                        Style="{StaticResource FlatBtn}" FontSize="10" Padding="10,4"
                                        Margin="4,0,0,0" Click="BrowsePreConnect_Click"/>
                                <ToggleButton Grid.Column="2" x:Name="PreConnectEmbed"
                                              Content="{Binding [BtnEmbed], Source={x:Static local:Lang.Instance}}"
                                              FontFamily="{DynamicResource Theme.FontFamily}" FontSize="10"
                                              Padding="10,4" Margin="4,0,0,0"
                                              Style="{StaticResource ToggleSwitch}"
                                              Width="Auto" Height="Auto"
                                              Checked="EmbedToggle_Changed" Unchecked="EmbedToggle_Changed"
                                              Tag="PreConnect"/>
                            </Grid>
                            <TextBox x:Name="PreConnectEmbedBox"
                                     FontFamily="Consolas" FontSize="10"
                                     AcceptsReturn="True" AcceptsTab="True"
                                     VerticalScrollBarVisibility="Auto" Height="80"
                                     Visibility="Collapsed" Margin="0,0,0,14"/>

                            <!-- Post-connect -->
                            <TextBlock Text="{Binding [TunnelScriptPostConnect], Source={x:Static local:Lang.Instance}}"
                                       FontFamily="{DynamicResource Theme.FontFamily}" FontSize="10"
                                       FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"
                                       Margin="0,0,0,4"/>
                            <Grid Margin="0,0,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" x:Name="PostConnectBox"
                                         FontFamily="{DynamicResource Theme.FontFamily}" FontSize="11"
                                         Padding="6,4"/>
                                <Button Grid.Column="1" Content="{Binding [BtnBrowse], Source={x:Static local:Lang.Instance}}"
                                        Style="{StaticResource FlatBtn}" FontSize="10" Padding="10,4"
                                        Margin="4,0,0,0" Click="BrowsePostConnect_Click"/>
                                <ToggleButton Grid.Column="2" x:Name="PostConnectEmbed"
                                              Content="{Binding [BtnEmbed], Source={x:Static local:Lang.Instance}}"
                                              FontFamily="{DynamicResource Theme.FontFamily}" FontSize="10"
                                              Padding="10,4" Margin="4,0,0,0"
                                              Style="{StaticResource ToggleSwitch}"
                                              Width="Auto" Height="Auto"
                                              Checked="EmbedToggle_Changed" Unchecked="EmbedToggle_Changed"
                                              Tag="PostConnect"/>
                            </Grid>
                            <TextBox x:Name="PostConnectEmbedBox"
                                     FontFamily="Consolas" FontSize="10"
                                     AcceptsReturn="True" AcceptsTab="True"
                                     VerticalScrollBarVisibility="Auto" Height="80"
                                     Visibility="Collapsed" Margin="0,0,0,14"/>

                            <!-- Pre-disconnect -->
                            <TextBlock Text="{Binding [TunnelScriptPreDisconnect], Source={x:Static local:Lang.Instance}}"
                                       FontFamily="{DynamicResource Theme.FontFamily}" FontSize="10"
                                       FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"
                                       Margin="0,0,0,4"/>
                            <Grid Margin="0,0,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" x:Name="PreDisconnectBox"
                                         FontFamily="{DynamicResource Theme.FontFamily}" FontSize="11"
                                         Padding="6,4"/>
                                <Button Grid.Column="1" Content="{Binding [BtnBrowse], Source={x:Static local:Lang.Instance}}"
                                        Style="{StaticResource FlatBtn}" FontSize="10" Padding="10,4"
                                        Margin="4,0,0,0" Click="BrowsePreDisconnect_Click"/>
                                <ToggleButton Grid.Column="2" x:Name="PreDisconnectEmbed"
                                              Content="{Binding [BtnEmbed], Source={x:Static local:Lang.Instance}}"
                                              FontFamily="{DynamicResource Theme.FontFamily}" FontSize="10"
                                              Padding="10,4" Margin="4,0,0,0"
                                              Style="{StaticResource ToggleSwitch}"
                                              Width="Auto" Height="Auto"
                                              Checked="EmbedToggle_Changed" Unchecked="EmbedToggle_Changed"
                                              Tag="PreDisconnect"/>
                            </Grid>
                            <TextBox x:Name="PreDisconnectEmbedBox"
                                     FontFamily="Consolas" FontSize="10"
                                     AcceptsReturn="True" AcceptsTab="True"
                                     VerticalScrollBarVisibility="Auto" Height="80"
                                     Visibility="Collapsed" Margin="0,0,0,14"/>

                            <!-- Post-disconnect -->
                            <TextBlock Text="{Binding [TunnelScriptPostDisconnect], Source={x:Static local:Lang.Instance}}"
                                       FontFamily="{DynamicResource Theme.FontFamily}" FontSize="10"
                                       FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"
                                       Margin="0,0,0,4"/>
                            <Grid Margin="0,0,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" x:Name="PostDisconnectBox"
                                         FontFamily="{DynamicResource Theme.FontFamily}" FontSize="11"
                                         Padding="6,4"/>
                                <Button Grid.Column="1" Content="{Binding [BtnBrowse], Source={x:Static local:Lang.Instance}}"
                                        Style="{StaticResource FlatBtn}" FontSize="10" Padding="10,4"
                                        Margin="4,0,0,0" Click="BrowsePostDisconnect_Click"/>
                                <ToggleButton Grid.Column="2" x:Name="PostDisconnectEmbed"
                                              Content="{Binding [BtnEmbed], Source={x:Static local:Lang.Instance}}"
                                              FontFamily="{DynamicResource Theme.FontFamily}" FontSize="10"
                                              Padding="10,4" Margin="4,0,0,0"
                                              Style="{StaticResource ToggleSwitch}"
                                              Width="Auto" Height="Auto"
                                              Checked="EmbedToggle_Changed" Unchecked="EmbedToggle_Changed"
                                              Tag="PostDisconnect"/>
                            </Grid>
                            <TextBox x:Name="PostDisconnectEmbedBox"
                                     FontFamily="Consolas" FontSize="10"
                                     AcceptsReturn="True" AcceptsTab="True"
                                     VerticalScrollBarVisibility="Auto" Height="80"
                                     Visibility="Collapsed" Margin="0,0,0,8"/>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <!-- Options tab — behaviour toggles + data cap (moved out of the footer) -->
                <TabItem x:Name="TabOptions"
                         Header="{Binding [TunnelDialogTabOptions], Source={x:Static local:Lang.Instance}}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20,16">
                        <StackPanel>
                            <!-- Default action -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                                <ToggleButton x:Name="IsDefaultToggle"
                                              Style="{StaticResource ToggleSwitch}"
                                              Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="⚡ Default action"
                                           FontFamily="{DynamicResource Theme.FontFamily}" FontSize="12"
                                           Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
                            </StackPanel>

                            <!-- Open network protection -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                                <ToggleButton x:Name="IsOpenProtectionToggle"
                                              Style="{StaticResource ToggleSwitch}"
                                              Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="🔓 Open network protection"
                                           FontFamily="{DynamicResource Theme.FontFamily}" FontSize="12"
                                           Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
                            </StackPanel>

                            <!-- Kill switch -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                                <ToggleButton x:Name="KillSwitchToggle"
                                              Style="{StaticResource ToggleSwitch}"
                                              Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="KillSwitchToggleLabel" Text="🔒 Kill switch"
                                           FontFamily="{DynamicResource Theme.FontFamily}" FontSize="12"
                                           Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
                            </StackPanel>

                            <!-- Auto-reconnect -->
                            <StackPanel x:Name="AutoReconnectRow" Orientation="Horizontal" Margin="0,0,0,14">
                                <ToggleButton x:Name="AutoReconnectToggle"
                                              Style="{StaticResource ToggleSwitch}"
                                              Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="AutoReconnectToggleLabel" Text="🔄 Auto-reconnect"
                                           FontFamily="{DynamicResource Theme.FontFamily}" FontSize="12"
                                           Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
                            </StackPanel>

                            <!-- Data-usage warnings (day / week / month) -->
                            <TextBlock Text="{Binding [TunnelDialogDataCapsHeader], Source={x:Static local:Lang.Instance}}"
                                       Style="{StaticResource SectionLabel}" Margin="0,12,0,6"/>
                            <TextBlock Text="{Binding [TunnelDialogDataCapsHint], Source={x:Static local:Lang.Instance}}"
                                       FontFamily="{DynamicResource Theme.FontFamily}" FontSize="9"
                                       Foreground="{DynamicResource TextMuted}" TextWrapping="Wrap"
                                       Margin="0,0,0,8"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <TextBlock Text="{Binding [TunnelDialogCapDaily], Source={x:Static local:Lang.Instance}}"
                                           FontFamily="{DynamicResource Theme.FontFamily}" FontSize="12" Width="150"
                                           Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
                                <TextBox x:Name="DailyCapBox" Width="80" FontSize="11" Text="0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="DailyUsedLabel" FontSize="10" Foreground="{DynamicResource TextMuted}"
                                           VerticalAlignment="Center" Margin="8,0,0,0"/>
                                <CheckBox x:Name="DailyCapKillChk" Margin="12,0,0,0"
                                          FontSize="11" VerticalAlignment="Center"
                                          Content="{Binding [TunnelDialogCapKill], Source={x:Static local:Lang.Instance}}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <TextBlock Text="{Binding [TunnelDialogCapWeekly], Source={x:Static local:Lang.Instance}}"
                                           FontFamily="{DynamicResource Theme.FontFamily}" FontSize="12" Width="150"
                                           Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
                                <TextBox x:Name="WeeklyCapBox" Width="80" FontSize="11" Text="0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="WeeklyUsedLabel" FontSize="10" Foreground="{DynamicResource TextMuted}"
                                           VerticalAlignment="Center" Margin="8,0,0,0"/>
                                <CheckBox x:Name="WeeklyCapKillChk" Margin="12,0,0,0"
                                          FontSize="11" VerticalAlignment="Center"
                                          Content="{Binding [TunnelDialogCapKill], Source={x:Static local:Lang.Instance}}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding [TunnelDialogCapMonthly], Source={x:Static local:Lang.Instance}}"
                                           FontFamily="{DynamicResource Theme.FontFamily}" FontSize="12" Width="150"
                                           Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
                                <TextBox x:Name="MonthlyCapBox" Width="80" FontSize="11" Text="0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="MonthlyUsedLabel" FontSize="10" Foreground="{DynamicResource TextMuted}"
                                           VerticalAlignment="Center" Margin="8,0,0,0"/>
                                <CheckBox x:Name="MonthlyCapKillChk" Margin="12,0,0,0"
                                          FontSize="11" VerticalAlignment="Center"
                                          Content="{Binding [TunnelDialogCapKill], Source={x:Static local:Lang.Instance}}"/>
                            </StackPanel>
                            <CheckBox x:Name="HideCapRingChk" Margin="0,10,0,0"
                                      FontSize="11" VerticalAlignment="Center"
                                      Content="{Binding [TunnelDialogHideCapRing], Source={x:Static local:Lang.Instance}}"/>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <!-- Split tunneling tab (local tunnels only) -->
                <TabItem x:Name="TabSplit"
                         Header="{Binding [TunnelDialogTabSplit], Source={x:Static local:Lang.Instance}}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20,16">
                        <StackPanel>
                            <TextBlock Text="{Binding [SplitSectionHeader], Source={x:Static local:Lang.Instance}}"
                                       Style="{StaticResource SectionLabel}" Margin="0,0,0,6"/>
                            <TextBlock Text="{Binding [SplitSectionHint], Source={x:Static local:Lang.Instance}}"
                                       FontFamily="{DynamicResource Theme.FontFamily}" FontSize="9"
                                       Foreground="{DynamicResource TextMuted}" TextWrapping="Wrap"
                                       Margin="0,0,0,10"/>

                            <!-- Mode -->
                            <StackPanel>
                                <RadioButton x:Name="SplitModeOffRadio" GroupName="SplitMode" IsChecked="True"
                                             Content="{Binding [SplitModeOff], Source={x:Static local:Lang.Instance}}"
                                             Foreground="{DynamicResource TextPrimary}"
                                             FontFamily="{DynamicResource Theme.FontFamily}" FontSize="11"
                                             Margin="0,0,0,4" Checked="SplitMode_Changed"/>
                                <RadioButton x:Name="SplitModeExcludeRadio" GroupName="SplitMode"
                                             Content="{Binding [SplitModeExclude], Source={x:Static local:Lang.Instance}}"
                                             Foreground="{DynamicResource TextPrimary}"
                                             FontFamily="{DynamicResource Theme.FontFamily}" FontSize="11"
                                             Margin="0,0,0,4" Checked="SplitMode_Changed"/>
                                <RadioButton x:Name="SplitModeIncludeRadio" GroupName="SplitMode"
                                             Content="{Binding [SplitModeInclude], Source={x:Static local:Lang.Instance}}"
                                             Foreground="{DynamicResource TextPrimary}"
                                             FontFamily="{DynamicResource Theme.FontFamily}" FontSize="11"
                                             Margin="0,0,0,4" Checked="SplitMode_Changed"/>
                            </StackPanel>

                            <!-- Ranges -->
                            <TextBlock Text="{Binding [SplitRangesLabel], Source={x:Static local:Lang.Instance}}"
                                       Style="{StaticResource SectionLabel}" Margin="0,12,0,4"/>
                            <TextBlock Text="{Binding [SplitRangesHint], Source={x:Static local:Lang.Instance}}"
                                       FontFamily="{DynamicResource Theme.FontFamily}" FontSize="9"
                                       Foreground="{DynamicResource TextMuted}" TextWrapping="Wrap"
                                       Margin="0,0,0,6"/>
                            <TextBox x:Name="SplitRangesBox" IsEnabled="False"
                                     AcceptsReturn="True" MinHeight="90" MaxHeight="160"
                                     FontFamily="{DynamicResource Theme.FontFamily}" FontSize="11"
                                     VerticalScrollBarVisibility="Auto" TextWrapping="NoWrap"/>

                            <!-- Apps (coming in a later 4.x) -->
                            <TextBlock Text="{Binding [SplitAppsLabel], Source={x:Static local:Lang.Instance}}"
                                       Style="{StaticResource SectionLabel}" Margin="0,14,0,4"/>
                            <TextBlock Text="{Binding [SplitAppsComingSoon], Source={x:Static local:Lang.Instance}}"
                                       FontFamily="{DynamicResource Theme.FontFamily}" FontSize="9" FontStyle="Italic"
                                       Foreground="{DynamicResource TextMuted}" TextWrapping="Wrap"
                                       Margin="0,0,0,6"/>
                            <TextBox x:Name="SplitAppsBox" IsEnabled="False"
                                     AcceptsReturn="True" MinHeight="50" MaxHeight="90"
                                     FontFamily="{DynamicResource Theme.FontFamily}" FontSize="11"
                                     VerticalScrollBarVisibility="Auto" TextWrapping="NoWrap"/>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <!-- Raw config tab -->
                <TabItem x:Name="TabRaw"
                         Header="{Binding [TunnelDialogTabRaw], Source={x:Static local:Lang.Instance}}"
                         GotFocus="TabRaw_GotFocus">
                    <Grid Margin="12">
                        <TextBox x:Name="RawBox"
                                 AcceptsReturn="True" AcceptsTab="True"
                                 FontFamily="{DynamicResource Theme.FontFamily}" FontSize="11"
                                 VerticalScrollBarVisibility="Auto"
                                 HorizontalScrollBarVisibility="Auto"
                                 TextWrapping="NoWrap"
                                 VerticalAlignment="Stretch"/>
                    </Grid>
                </TabItem>
            </TabControl>

            <!-- Button bar — toggles now live on the Options tab -->
            <Border Grid.Row="3" Background="{DynamicResource Surface}" CornerRadius="{DynamicResource Theme.CornerRadiusBottom}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                            VerticalAlignment="Center" Margin="16,10">
                    <Button Content="{Binding [BtnCancel], Source={x:Static local:Lang.Instance}}"
                            Style="{StaticResource FlatBtn}" Padding="20,6" Margin="0,0,8,0"
                            Click="CancelBtn_Click"/>
                    <Button Content="{Binding [BtnSaveTunnel], Source={x:Static local:Lang.Instance}}"
                            Style="{StaticResource PrimaryBtn}" Padding="20,6"
                            Click="SaveBtn_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
